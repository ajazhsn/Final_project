global:
  resolve_timeout: 5m
  smtp_smarthost: 'sandbox.smtp.mailtrap.io:587'
  smtp_from: 'plantdisease@mlops.com'
  smtp_auth_username: '0efd76baea3c67'
  smtp_auth_password: 'd5457ff66c0532'
  smtp_require_tls: true

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h
  receiver: 'email'

receivers:
  - name: 'email'
    email_configs:
      - to: 'da25m006@smail.iitm.ac.in'
        send_resolved: true
        require_tls: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname']